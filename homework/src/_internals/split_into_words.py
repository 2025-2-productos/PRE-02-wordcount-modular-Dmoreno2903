def split_into_words(line: str) -> list[str]:
    """Split a line into words
    
    Args:
        line: the line to split into words
        
    Returns:
        list of words
    """
    return line.split()